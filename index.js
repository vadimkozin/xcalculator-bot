require('dotenv').load();
require('./src/bot');
//require('./src/web');   // чтобы heroku не ругался вот так: Error R10 (Boot timeout) -> Web process failed to bind to $PORT within 60 seconds of launch
